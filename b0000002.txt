GITLAB_URL=https://gitlab.example.com
GITLAB_PAT=xxxxxxxxxxxxxxxxxxxx
MCP_HOST=0.0.0.0
MCP_PORT=8000
# Curated project list for the cross-project search tools (optional; this is the default)
GITLAB_WIKI_PROJECTS_FILE=wiki_projects.toml
# Search cache snapshot restored on restart (optional; this is the default — set empty to disable)
GITLAB_SEARCH_CACHE_FILE=.search_cache.json
# Expose the mutating tools (create/update/comment). OFF by default: the server is
# read-only unless you set this to 1/true/yes/on. Leave unset for onboarding/read use.
GITLAB_MCP_ENABLE_WRITES=0
# Log every tool dispatch (name, arguments, outcome, duration) to triage failing
# calls. OFF by default (1/true/yes/on to enable).
GITLAB_MCP_LOG_TOOL_CALLS=0
# Log verbosity when the above is on (optional; default INFO). DEBUG also logs a line
# at call entry, useful if a call hangs.
GITLAB_MCP_LOG_LEVEL=INFO
# Persist the tool-call log to a rotating file as well as stderr (optional; unset =
# stderr only). Rotates at 5 MB, keeping 3 backups.
GITLAB_MCP_LOG_FILE=
